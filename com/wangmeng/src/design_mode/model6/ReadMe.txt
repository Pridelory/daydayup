单例设计模式

概念：确保一个类只有一个实例，并提供一个全局的访问点。

懒汉式：预先不初始化，不占用资源。

饿汉式：预先初始化，加载实例，带来的后果是万一实例特别耗费资源，那么这种就不是好的设计模式，如果对于资源没要求，那么可以用。

多线程下懒汉式的缺点
可能同时生成两个不一样的实例

解决办法：
1、同步懒汉式，缺点是性能下降较快
2、用饿汉式，缺点是占用内存
3、双检查加锁，在getInstance()中减少使用同步。代码见图片（双检查加锁）
4、内部类

